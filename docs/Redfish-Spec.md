# Redfish Spec Summary (generated from repo)

This document is an initial, human-friendly summary of the Redfish schemas and Redfish-related implementation found inside this repository (panyue8/bmcweb). It was generated from the repository's existing schema files and docs (not from the DMTF website).

Files discovered and summarized:

- redfish-core/schema/dmtf/csdl/
  - RedfishError_v1.xml — Defines RedfishError and RedfishErrorContents complex types (code, message). Used for error payloads.
  - Pump_v1.xml — Pump schema (pump unit for cooling systems), includes EntityType Pump deriving from Resource.
  - Protocol_v1.xml — Protocol enumeration (PCIe, AHCI, UHCI, SAS, SATA, USB, etc.).
  - Manifest_v1.xml — Manifest schema with types like Request and Response and revision annotations.
  - RedfishExtensions_v1.xml — Redfish-specific schema language extensions (terms like AllowableValues, AllowableNumbers, AllowablePattern, WriteableProperties).
  - (Other csdl files may exist in the directory; this summary focuses on the most relevant files found.)

- redfish-core/lib/
  - redfish_v1.hpp — Implementation of JsonSchemas collection endpoints and jsonSchema file accessors (/redfish/v1/JsonSchemas and /redfish/v1/JsonSchemas/{schema}).
  - redfish_sessions.hpp — SessionService implementation and routes (SessionTimeout, Sessions collection handling).

- scripts/
  - generate_schema_collections.py — Script to parse CSDL to determine top-level collection URIs for aggregation (generates aggregation_utils.hpp).
  - update_schemas.py — Script to update local schema files from an external Redfish Publications repository (NOTE: present but our summary uses only files included in this repo).

- docs/Redfish.md — Existing documentation listing Redfish URIs and targeted fields for OpenBMC. Useful source for endpoint-level details.

Summary contents (this file):

1) Table of contents
2) For each schema discovered: short description, key properties/fields (where applicable), and important notes or references to implementation files.
3) High-level mapping: which repo files implement which Redfish endpoints (examples: SessionService, JsonSchemas index).
4) Next steps: convert this markdown to a Word document (.docx) if you want a formal Word file; or expand the doc to include full schema XML excerpts.

---

## 1) Table of contents

- RedfishError (redfish-core/schema/dmtf/csdl/RedfishError_v1.xml)
- Pump (redfish-core/schema/dmtf/csdl/Pump_v1.xml)
- Protocol (redfish-core/schema/dmtf/csdl/Protocol_v1.xml)
- Manifest (redfish-core/schema/dmtf/csdl/Manifest_v1.xml)
- RedfishExtensions (redfish-core/schema/dmtf/csdl/RedfishExtensions_v1.xml)
- Implementation notes (redfish-core/lib/*)
- Scripts (scripts/*)

## 2) Schema summaries

### RedfishError_v1.xml
- Purpose: Defines the error payload types used by the service.
- Key types:
  - RedfishError (contains property `error` of type RedfishErrorContents)
  - RedfishErrorContents:
    - code (Edm.String, required) — MessageId from a message registry
    - message (Edm.String, required) — Human-readable message
- Notes: Present versions v1_0_0..v1_0_2 in the file; used by the service to generate standard responses.

### Pump_v1.xml
- Purpose: Schema describing a pump unit for cooling systems.
- Key aspects:
  - EntityType `Pump` derives from Resource.v1_0_0.Resource and is marked abstract (schema contains properties and annotations describing pumps).
- Notes: Useful for thermal/cooling device modeling if OpenBMC exposes pump devices.

### Protocol_v1.xml
- Purpose: Enumeration of protocols for storage/fabric models.
- Key enum members include: PCIe, AHCI, UHCI, SAS, SATA, USB, etc.
- Notes: Reference for any endpoint or property that exposes protocol types for devices.

### Manifest_v1.xml
- Purpose: Models manifests and includes ComplexTypes Request and Response which allow additional properties describing stanza contents.
- Notes: Includes revision annotations and multiple versions (v1_0_0..v1_1_2). Useful for management tasks and manifests handling.

### RedfishExtensions_v1.xml
- Purpose: Additional Redfish-specific terms/annotations extending the schema language.
- Key terms:
  - AllowableValues, AllowableNumbers, AllowablePattern, WriteableProperties
- Notes: These terms appear as annotations in other schemas to describe allowable values, writable properties, etc.

## 3) Implementation mapping
- JsonSchemas endpoints implemented in redfish-core/lib/redfish_v1.hpp — serves /redfish/v1/JsonSchemas and individual JsonSchemaFile resources. Reads JSON schema files from /usr/share/www/redfish/v1/JsonSchemas.
- SessionService implemented in redfish-core/lib/redfish_sessions.hpp — handles SessionService root, Sessions collection, session timeout patching, and per-session routes.
- Schema parsing helpers and aggregation utilities are generated by scripts/generate_schema_collections.py which parses local csdl to generate aggregation URIs.

## 4) Next steps (recommended)
- Review this markdown. If it looks good, I will convert it to a Word document (docs/Redfish-Spec.docx) and commit it to the repository. Converting to .docx will preserve the same content and include a table of contents and formatted sections.
- Optionally expand each schema section to include the full XML snippet or the JSON schema counterpart (may be large).
- Optionally place the .docx on a separate branch (recommended: `redfish-spec-doc`) if you prefer to keep main branch clean.

---

Generated automatically from files present in the repository. If you want me to include additional schema files or embed full XML excerpts, reply with your preferences and I'll update the document and commit again.
